import type { PaintRecord } from "../types";

/**
 * Generado por `npm run build:catalog`. Se tipa a mano en vez de dejar que
 * TypeScript infiera el literal: son 1.130 objetos y la inferencia completa
 * ralentiza el chequeo sin aportar nada.
 */
declare const catalog: PaintRecord[];
export default catalog;
